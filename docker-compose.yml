version: '3'
services:
  php-fpm:
    build:
      context: .
    ports:
      - "9000:9000"
  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php-fpm
